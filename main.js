const validaValorA = document.getElementById('campo-A');
const validaValorB = document.getElementById('campo-B');

function validaValor(' '); {
    formValido = validavalor(validaValorA.value > validaValorB.value);
    if (validaValorA.value > validaValorB.value) {
        alert ('Confirmado');
    }

    else {
        alert('Recusado');
    }
})